---
kind: pipeline
type: exec
name: deploy

platform:
  os: linux
  arch: amd64

workspace:
  path: /data/share/projects/git/ansible

steps:
- name: mirroring to github
  commands:
  - git push --mirror git@github.com:pwnlxrd/citadel.git
  - echo "mirroring done 👌"